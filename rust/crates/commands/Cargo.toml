[package]
name = "mg101-commands"
version.workspace = true
edition.workspace = true
license.workspace = true

# Magistrala komend — jeden rejestr operacji domenowych dla UI (Slint), agenta
# (tool-calling), MCP i przyszłego weba (ADR-0002, HLD §2). Port DomainCommand v1.
[dependencies]
serde_json = "1"

[lib]
name = "mg101_commands"
path = "src/lib.rs"
